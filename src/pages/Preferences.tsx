import React from 'react';
import { Empty } from 'antd';

const Preferences = (): JSX.Element => <Empty image={Empty.PRESENTED_IMAGE_SIMPLE} description="Preferences n/a" />;

export default Preferences;
